# Invalid scenario for validation error testing  
description   = "Invalid scenario for validation testing"
source_type   = "file"
source_name   = "some_file.csv"
target_column = "target"
drift_types   = ["invalid_drift_type"] # Invalid drift type to trigger validation error

[ref_filter]
    sample_range = [0, 100]

[test_filter]
    sample_range = [200, 300]

[ground_truth]
    drift_periods        = [[200, 300]]
    drift_intensity      = "medium"
    expected_effect_size = 0.5
